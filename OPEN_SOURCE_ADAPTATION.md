# 开源项目适配评估

日期: 2026-08-31
目标: OKX 现货为主 + 可选 funding carry 的个人量化系统。

## 结论

不整套引入股票量化框架。当前项目的瓶颈是数据口径、funding carry 成本模型、walk-forward 和执行安全，不是缺少一个大框架。保留自有轻量核心，用成熟项目的局部思想和组件。

| 项目 | 适配结论 | 可借鉴/可复用 | 当前不采用的原因 |
|---|---|---|---|
| Qlib | 研究方法借鉴, 暂不作为运行时核心 | 数据处理、因子表达、模型训练、滚动/时间序列切分、实验记录 | 默认生态偏股票横截面和日频; 接 OKX/funding 需自写数据适配; 引入会增加数据 schema 和依赖复杂度 |
| vn.py | 执行/事件架构借鉴, P4 再评估 | 事件驱动、订单状态、成交回报、对账、网关抽象 | 当前只有 BTC 现货纸面盘; 直接引入对 $500 试点过重; OKX 现货/永续网关需实际验证，不能只看项目星数 |
| AKShare | 不作为 OKX 主数据源 | 股票/宏观/基本面数据补充(若以后做跨资产) | 它不是交易执行框架，OKX crypto/funding 不是主强项; 当前已有 OKX API + Coin Metrics + F&G |
| Lean | 仅在多资产/高保真事件回测时考虑 | 事件驱动撮合、订单生命周期、组合与费用模型 | C#/Python 双栈、数据和券商适配成本高; 当前日线策略用轻量回测足够; OKX 适配必须单独验证 |
| easytrader | 不适用 | 无 | 面向同花顺/miniQMT/雪球等 A 股执行，不是 OKX crypto |
| PanWatch | 监控/报告层强借鉴, 不接交易决策 | 持仓汇总、净值、提醒、PWA/IM 推送、把研究结论写成决策卡片 | 面向 A/H/美股; 其 TradingAgents 集成是研究辅助，不等于可证明盈利的策略; 直接部署会引入前端/数据库范围 |
| TradingAgents | 研究辅助层可选, 不进自动下单链 | 多角色观点分离、风险审查、决策记录、可解释研究报告 | LLM 输出不可当稳定 alpha; 可能有延迟、幻觉、成本和提示漂移; 对 1h/日线策略只适合作为低频旁路意见 |
| Kronos | 预测模型挑战者, 暂不作为生产信号 | 用统一接口做严格 OOS 的 next-bar/next-day 预测基准，与趋势因子比较 | foundation model 的回测结果易被数据泄漏/域迁移/成本忽略误导; 价格预测不等于可交易收益; 需核对训练数据、切分、许可证和推理资源 |

## 推荐采用顺序

### A. 立即采用(不改变当前交易规则)

1. **Qlib 思想**: 实验目录、特征快照、固定 train/validation/test 时间切分、每次实验保存参数和成本假设。当前 `backtest.py`/`p2_research.py` 继续保留，增加实验 manifest 即可。
2. **PanWatch 思想**: 把 `data/paper/report.md` 扩展为状态面板: 净值、当前/目标仓位、最近决策、数据新鲜度、异常和回撤告警。
3. **TradingAgents 思想**: 仅在每日复盘生成一份研究 memo，输入冻结的行情/因子摘要，输出观点和反证；不改变自动仓位，不允许直接下单。

### B. P3/P4 再做

4. **vn.py/Lean 的执行纪律**: 先把订单状态机、幂等 client order id、成交回报、重启对账加入自有代码；只有需要多个品种、分钟级事件回测或多个交易所时再评估引入框架。
5. **Kronos challenger**: 单独输出预测，不参与账户；与 `trend_only` 做同一时间切分、同一手续费/滑点、同一信号延迟，验收标准是 OOS 可交易收益和回撤，而不是方向准确率。
6. **AKShare/easytrader**: 只有增加 A 股账户时再引入，与当前 OKX 项目隔离。

## 不可绕过的验收门槛

- 所有候选模型只使用当时可获得的数据，严格次日成交，禁止未来函数。
- 必须扣手续费、滑点、资金费率、换手和数据延迟；只报净收益，不报裸预测准确率。
- 固定最后样本外区间，研究过程中不得反复查看并调参。
- 交易规则变更前，在纸面账户并行运行至少 4 周，并记录旧规则与新规则的差异。
- LLM/Kronos 任何情况下都不能绕过仓位上限、熔断、对账和人工暂停开关。

## 对当前项目的最终架构建议

```
OKX/Deribit/Coin Metrics/F&G
          ↓
自有标准化数据层 + 实验 manifest (吸收 Qlib 方法)
          ↓
趋势/carry 因子与轻量回测
          ├── 生产候选: 固定规则 + 风控
          ├── Kronos challenger(旁路, 不下单)
          └── TradingAgents/PanWatch 研究与监控旁路
          ↓
paper account → OKX demo → 小额实盘
```

当前不改变 `$500` 账户的 v0_full 规则，也不引入任何真实 API key 或自动订单。
