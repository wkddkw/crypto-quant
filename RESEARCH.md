# 一键离线研究

入口：`research_runner.py`。只读取本地 OKX 日线，在 `data/research/` 保存独立实验，
不请求外部接口、不下单、不写已有纸面账本、不修改 `strategy_registry.json` 或调度器。

## 使用

```bash
.venv/bin/python research_runner.py
```

默认一次运行 BTC/ETH、4 个固定候选、3 个情景，共 24 组。
不用安装 Freqtrade、Hummingbot 或 VectorBT：本轮借鉴验证方法，计算继续使用项目已有 pandas/NumPy。
依赖沿用 `requirements.txt`，包括读取 Parquet 所需的 pyarrow。

```bash
# 从已冻结的数据和配置重跑；仍生成全新的独立实验目录。
.venv/bin/python research_runner.py \
  --data-dir data/research/<experiment_id>/snapshots \
  --config data/research/<experiment_id>/snapshots/research_config.json

# 可选：每个独立模拟账户的月运行费用，用于单独展示费用后损益。
.venv/bin/python research_runner.py --monthly-running-cost 10
```

费用参数是对服务器/API 等外部开销的情景输入，不代表实际账单。
它按验证期间天数折算为外部支出，不逐日扣减模拟账户的可投资现金。
未提供时显示“未提供成本”，不能把扣交易成本收益称为项目净收入。

远程使用本命令前，先完成代码部署。若采集任务可能同时更新行情文件，使用当前部署手册中
的观察锁读取快照；本命令不自行安装或修改 cron，也不替代小时观察任务。

## 固定实验

参数在 `research_config.json`，每轮保存原始配置和哈希。
修改它只影响后续独立研究，不能据此修改已冻结的策略或重写旧实验结果。

| 候选 | 定义 |
|---|---|
| buy_hold | 验证起点一次性买入，计入手续费与滑点，之后持有 |
| trend_only | 与独立趋势模拟盘同口径的 MA50/200、90 日波动调整动量、20 日突破，long/flat |
| ma200 | 收盘价高于 MA200 时持有，否则空仓 |
| trend_vol_cap | trend_only 乘以 min(1, 30% / 30 日实现年化波动率)，只减仓，不加杠杆 |

30% 是首轮事前固定的研究假设，没有从当前结果反向选择最优值。

| 情景 | 定义 |
|---|---|
| base | 单边手续费 0.1%、滑点 0.03%，日线收盘后下一根开盘执行 |
| slippage_x2 | 单边滑点加倍至 0.06%，手续费不变 |
| signal_delay_1d | 使用额外滞后一天的信号，按实际执行日开盘计价；不是分钟延迟模型 |

起始资金 $500、调仓偏差 10 个百分点、最小成交 $1，按成交时刻权重计算。
完整交易链按目标仓位和实际可用现金更新；现金不透支，现货不做空。
与旧 `backtest.py` 相比，新流程修正了基准起点、首笔成本与成交时刻权重口径，旧报告不会覆盖。
离线测试验证研究引擎与 `trend_paper.advance` 在相同价格/目标下的现金、持仓和费用一致。
实际线上 ticker 时刻与日线开盘仍可能不同，这不是已完成的线上逐笔成交对账。

## 时间切分和统计

- 默认从 2022 年开始按自然年展示验证；每折附上此前 3 年独立训练窗口结果，不据此选策略。
- 所有候选参数冻结。验证账户跨年连续运行，不在每年重置资金或免除换仓费用。
- 年度收益用前一年末权益作为起点；连续总收益/复合年化直接取连续账户曲线。
- 不足一年的窗口不输出年化；期末持仓按最后收盘估值，不强制清仓。
- 所有已看过的历史都标为历史验证，不声称拥有未触碰的最终 OOS。
- 候选复核仅列历史收益/回撤的通过项和不足，不修改注册表状态，不等于晋级。
- 回撤由日度收盘估值计算，不能代表日内最坏损失。

## 数据与偏差检查

日线必须 UTC 对齐、连续、无重复，品种与文件名匹配，OHLC 有限且范围有效。
未收盘日线剔除；历史文件过期会明确显示距最近收盘时间，但允许用于历史研究。
缺口/重复/非法价格会让实验失败归档，不补造价格和成交。

借鉴 Freqtrade `lookahead-analysis` / `recursive-analysis` 的方法：
对最多 24 个历史时点分别用完整序列、截止当时的前缀、最近 201 根预热数据计算目标，检查差异。
这些是抽样指标检查，不是对全部行情、信号和执行路径不存在未来数据的完整证明。

## 实验文件

```text
data/research/<UTC timestamp>-<random id>/
  manifest.json       # 成败、代码/配置/行情哈希、版本、来源、时效、耗时、可选运行成本
  checks.json         # 行情质量和因果/预热检查
  splits.json         # 每年训练与验证的时间、索引、是否不足完整年
  comparison.json     # 各候选/情景的连续结果和逐年结果
  report.md           # 比较表和局限
  snapshots/          # 原始行情、研究配置、入口代码、provenance、注册表快照
  BTC-USDT/
    equity.csv        # 每个候选/情景的连续净值和持仓
    trades.csv        # 信号/成交时点、价格、数量、费用、滑点
  ETH-USDT/
    equity.csv
    trades.csv
```

先写临时目录，完成或失败后整目录改名。失败实验 `manifest.status=failed`，不伪装成功报告。
代码、参数或行情变更必须另建实验；原始输入与正式纸面账本不会改写。
数据获取时间未知时显式为 null，快照读取时间不能冒充交易所数据最初采集时间。

## 参考与后续

调研与官方来源见 [开源方案评估](docs/research/2026-09-05-open-source-iteration-review.md)。
本轮已实现趋势研究入口、完整证据包和成本/延迟检查。Carry 多品种机会研究、真实订单簿执行、
远程历史成交逐笔对账是后续独立任务，不能从本报告推导其盈利或完成状态。
